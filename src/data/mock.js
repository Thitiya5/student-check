/**
 * @deprecated Import from `./appState.js` instead.
 * Kept for backward compatibility with older imports.
 */
export {
  STORAGE_KEY,
  loadAppState,
  getDefaultAppState,
  getTodayDate,
  getTodayDateKey,
  saveAppState
} from './appState.js';
